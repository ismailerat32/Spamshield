<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<title>Analiz</title>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<style>
body {
    margin:0;
    font-family: Arial;
    background: radial-gradient(circle at top, #0f2027, #203a43, #2c5364);
    color:#fff;
}

.header {
    text-align:center;
    padding:20px;
    font-size:22px;
    font-weight:bold;
    letter-spacing:1px;
}

.grid {
    display:grid;
    grid-template-columns: repeat(2,1fr);
    gap:15px;
    padding:15px;
}


.card:hover{
  transform:translateY(-3px);
  box-shadow:0 0 20px rgba(0,255,200,0.15);
}

  background:linear-gradient(135deg, rgba(0,255,200,0.08), rgba(0,0,0,0.4));
  box-shadow:0 0 25px rgba(0,255,200,0.08);
  padding:12px;
  border-radius:12px;
}

@keyframes pulse{
  0%{opacity:0.6}
  50%{opacity:1}
  100%{opacity:0.6}
}


.card {
    background: rgba(0,0,0,0.4);
    border-radius:15px;
    padding:15px;
    box-shadow: 0 0 15px rgba(0,255,255,0.2);
}

.card h3 {
    margin:0 0 10px;
    font-size:14px;
}

.value {
    font-size:22px;
    font-weight:bold;
}

.bar {
  height:6px;
  border-radius:10px;
  overflow:hidden;
  margin-top:6px;
}

.bar span {
  display:block;
  height:100%;
  background:linear-gradient(90deg, #00ffd0, #00ffa2);
  box-shadow:0 0 10px #00ffd0;
  transition:0.6s;
}

/* GRAFİK */
.chart-box {
    margin:15px;
    padding:15px;
    background: rgba(0,0,0,0.35);
    border-radius:15px;
    box-shadow:0 0 25px rgba(0,255,200,0.08);
}

/* BUTTON */
.back {
  display:block;
  margin:20px auto;
  width:200px;
  text-align:center;
  padding:10px;
  background:linear-gradient(90deg,#00ffd0,#00ffa2);
  color:#000;
  border-radius:20px;
  text-decoration:none;
  font-weight:bold;
  transition:0.25s;
}

.back:hover{
  box-shadow:0 0 15px #00ffd0;
  transform:scale(1.05);
}y


  margin:18px 15px 24px;
  padding:15px;
  border-radius:15px;
  box-shadow:0 0 15px rgba(0,255,255,0.18);
}
.bottom-panel h3{
  margin:0 0 12px;
  font-size:15px;
}
.mini-stats upgraded{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:10px;
  font-size:13px;
}
.mini-stats upgraded div{
  padding:10px;
  border-radius:10px;
  text-align:center;
}


.mini-stats.upgraded div{
  position:relative;
  overflow:hidden;
}

.mini-stats.upgraded div::after{
  content:"";
  position:absolute;
  inset:0;
  background:linear-gradient(120deg, transparent, rgba(0,255,200,0.15), transparent);
  opacity:0;
  transition:0.4s;
}

.mini-stats.upgraded div:hover::after{
  opacity:1;
}


@keyframes fadeUp{
  from{opacity:0; transform:translateY(15px);}
  to{opacity:1; transform:translateY(0);}
}



.mini-stats.fixed{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:12px;
}

.mini-stats.fixed div{
  border-radius:12px;
  padding:12px;
  text-align:center;
  font-size:13px;
}


  margin-top:15px;
  padding:18px;
  border-radius:18px;
  background:linear-gradient(145deg, rgba(0,0,0,0.4), rgba(0,255,200,0.08));
  box-shadow:0 0 20px rgba(0,255,200,0.15);
}

.bottom-panel h3{
  font-size:16px;
  margin-bottom:12px;
  color:#00ffc3;
}

.mini-stats{
  display:grid;
  grid-template-columns:repeat(3,1fr);
  gap:12px;
}

.mini-stats div{
  padding:14px;
  border-radius:12px;
  box-shadow:inset 0 0 8px rgba(0,255,200,0.1);
  font-size:13px;
}

.mini-stats b{
  display:block;
  margin-top:4px;
  font-size:16px;
  color:#00ffc3;
}


/* Sayfa yüksekliği ve boşluk */
body{
  min-height:100vh;
  display:flex;
  flex-direction:column;
}

/* Ana içerik ortalansın */
.container{
  flex:1;
}

/* Kartlar arası ritim */

/* Günlük Özet güçlendirme */
  margin-top:10px;
  padding:16px;
  border-radius:16px;
  border:1px solid rgba(0,255,200,0.15);
}

/* Başlık hizası */
.bottom-panel h3{
  font-size:15px;
  margin-bottom:10px;
  opacity:0.9;
}

/* Mini kutular */
.mini-stats div{
  padding:12px;
  border-radius:10px;
  font-size:13px;
}

/* Alt buton spacing */
.back{
  margin:16px auto 10px;
}



.card:hover{
  transform:translateY(-3px);
  box-shadow:0 0 20px rgba(0,255,200,0.15);
}

    },
    options: {
        plugins:{legend:{display:false}},
        scales:{
            x:{ticks:{color:"#fff"}},
            y:{ticks:{color:"#fff"}}
        }
    }
});
</script>

</body>
</html>



  backdrop-filter: blur(6px);
}

